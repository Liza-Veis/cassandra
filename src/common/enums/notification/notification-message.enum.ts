enum NotificationMessage {
  CQL_$TYPE_CANNOT_BE_REPRESENTED = 'CQL type {{type}} cannot be correctly represented in JSON Schema.',
  CQL_$TYPE_WAS_NOT_DEFINED = 'CQL type {{type}} was not defined.',
  $TABLE_JSON_SCHEMA_EXPORT_SUCCESS = '{{table}} table JSON Schema successfully exported.',
  $TABLE_JSON_SCHEMA_EXPORT_FAILURE = 'Failed to export {{table}} table JSON Schema.',
}

export { NotificationMessage };
